'use strict';

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

var ohHey = "Hello World";

console.log('hii');
console.log(ohHey);

var numger = [1, 2, 3];
var maxNum = Math.max.apply(Math, _toConsumableArray(numbers));

var string = 'ths is a ' + ohHey;

console.log(string);

console.log('hello');